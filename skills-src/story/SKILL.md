---
name: story
description: Reconstruct a session narrative from the shared memory layer.
argument-hint: [session id or query]
---

Use `memory_story(session_id=..., query=...)`.
Summarize the timeline, files, and major tool steps.
