---
name: digest
description: Generate a short activity digest from the shared memory layer.
argument-hint: [days]
---

Use `memory_digest(days=...)`.
Summarize projects, tool usage, and dominant activity types.
