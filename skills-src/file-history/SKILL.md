---
name: file-history
description: Trace mentions and edits for a file through the shared memory layer.
argument-hint: [path]
---

Use `memory_trace_file(path=...)`.
Show the most relevant sessions and explain why each matched.
