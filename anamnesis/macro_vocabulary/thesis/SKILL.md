---
name: thesis
description: Search for the origin of a decision or implementation choice.
argument-hint: "[query]"
---

Use `memory_search(query=...)` first.
Then use `memory_trace_decision(query=...)` to cluster the strongest matching sessions.
Assume the result came from the UQA-backed sidecar, because Anamnesis is UQA-only.
