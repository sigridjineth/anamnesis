---
name: cadence
description: Group sessions into time-based work cadences.
argument-hint: [days]
---

Use `memory_sprints(days=...)`.
Highlight project boundaries first, then summarize each cadence window.
